import { createSlice } from "@reduxjs/toolkit";

const initialState={
    value:0,
}

const Slicer =createSlice({
    name:data,
    initialState,
    reducers:{

        getRecentOrders(state){
            state.value=action
        }
    }
})

export const [getRecentOrders] =slice.actions

export default Slicer.reducer
